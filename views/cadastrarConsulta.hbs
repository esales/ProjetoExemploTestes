<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastrar Consulta</title>

    <style>

        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, Helvetica, sans-serif;
        }

        body{
            min-height: 100vh;
            background: linear-gradient(135deg, #0f172a, #1e293b);
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 30px;
        }

        .containerPrincipal{
            width: 100%;
            max-width: 700px;
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .tituloPagina{
            text-align: center;
            color: #0f172a;
            font-size: 2rem;
            margin-bottom: 30px;
        }

        .formulario{
            display: flex;
            flex-direction: column;
            gap: 22px;
        }

        .grupoCampo{
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .labelCampo{
            font-weight: bold;
            color: #334155;
        }

        .selectCampo,
        .inputCampo{
            padding: 14px;
            border: 1px solid #cbd5e1;
            border-radius: 10px;
            font-size: 1rem;
            transition: 0.3s;
        }

        .selectCampo:focus,
        .inputCampo:focus{
            outline: none;
            border-color: #2563eb;
            box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.15);
        }

        .grupoBotoes{
            display: flex;
            gap: 12px;
            margin-top: 10px;
        }

        .botao{
            flex: 1;
            border: none;
            padding: 14px;
            border-radius: 10px;
            font-size: 1rem;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
        }

        .botaoPrimario{
            background: #2563eb;
            color: white;
        }

        .botaoPrimario:hover{
            background: #1d4ed8;
        }

        .botaoSecundario{
            background: #e2e8f0;
            color: #0f172a;
            text-decoration: none;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .botaoSecundario:hover{
            background: #cbd5e1;
        }

        @media (max-width: 600px){

            .containerPrincipal{
                padding: 25px;
            }

            .grupoBotoes{
                flex-direction: column;
            }
        }

    </style>
</head>

<body>

    <div class="containerPrincipal">

        <h1 class="tituloPagina">
            Cadastrar Consulta
        </h1>

        <form action="/consulta" method="POST" class="formulario">

            <div class="grupoCampo">

                <label for="paciente" class="labelCampo">
                    Paciente
                </label>

                <select
                    id="paciente"
                    name="pacienteId"
                    required
                    class="selectCampo"
                >

                    {{#each pacientes}}

                        <option value="{{this.id}}">
                            {{this.nome}}
                        </option>

                    {{/each}}

                </select>

            </div>

            <div class="grupoCampo">

                <label for="medico" class="labelCampo">
                    Médico
                </label>

                <select
                    id="medico"
                    name="medicoId"
                    required
                    class="selectCampo"
                >

                    {{#each medicos}}

                        <option value="{{this.id}}">
                            {{this.nome}}
                        </option>

                    {{/each}}

                </select>

            </div>

            <div class="grupoCampo">

                <label for="hora" class="labelCampo">
                    Hora
                </label>

                <input
                    type="number"
                    id="hora"
                    name="hora"
                    required
                    class="inputCampo"
                    placeholder="Digite a hora da consulta"
                >

            </div>

            <div class="grupoBotoes">

                <button type="submit" class="botao botaoPrimario">
                    Cadastrar Consulta
                </button>

                <a href="/consulta" class="botao botaoSecundario">
                    Cancelar
                </a>

            </div>

        </form>

    </div>
    
</body>
</html>